export type RatingCreateFields = {
    referenceNumber:string
    slug:string
    rating:number
    description:string
}

export type RatingProductInfo = {
    slug: string
    name: string
    image?: string
    quantity: number
    subtotal: number
}

